# Core
httpx>=0.27.0
numpy>=1.24.0

# Web UI
fastapi>=0.100.0
uvicorn[standard]>=0.23.0

# Optional: For semantic embeddings via local model
# pip install sentence-transformers
# Set EMBEDDING_BACKEND=local to use
